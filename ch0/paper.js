let x = 3;
let y = x; // y holds the original value of x, which is 3
x = 5; // x is now changed to 5 while y remains 3
y = x; //y is now updated to the new value of x, which is 5

console.log(x, y); // -> 5 5

